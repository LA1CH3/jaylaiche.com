import { Button } from "@la1ch3/ui";

import { AppLayout } from "../../components/AppLayout";

export const meta = {
  title: "Rebuilding (again)",
  date: "August 17, 2021",
};

export default ({ children }) => <AppLayout meta={meta}>{children}</AppLayout>;

Here is a test paragraph.

And here is another.

Here is a [test link](google.com).

<Button>A button in MDX</Button>
